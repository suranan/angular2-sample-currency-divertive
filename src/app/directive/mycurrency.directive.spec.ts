import { MycurrencyDirective } from './mycurrency.directive';

describe('MycurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new MycurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
